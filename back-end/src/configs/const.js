const DB = {
  DB_USER :"postgres",
  DB_PASSWORD :"Apurupie123@",
  DB_NAME:'QLHS',
  DB_HOST:'localhost'
}
const PORT = {
	PORT_SERVER: 3000,
	PORT_DB: 5432,
	PORT_CLIENT: 5173,
};

module.exports = { DB, PORT };
